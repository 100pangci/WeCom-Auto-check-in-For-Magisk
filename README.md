# WeCom Auto Check-in For Magisk

自动在手机时间 08:10 和 17:30 打开企业微信考勤页面 `com.tencent.wework.enterprise.attendance.controller.AttendanceActivity2`。
支持跳过节假日，周末，并在调休日打卡。

## 安装方法

1. 下载release中的模块。
2. 在 Magisk 中安装该模块。
3. 重启手机。

## 特性

- 采用 `service.sh` 服务脚本在启动后常驻
- 在到达目标时间时自动唤醒屏幕并打开指定活动
- 保持屏幕锁定状态，不尝试绕过 Android 安全锁屏
- 可在 Magisk 模块列表中通过 `module.prop` 动态看到当前运行状态
- GitHub Actions 可直接构建未签名的 zip

## 状态显示

- 模块启动后会动态更新已安装模块目录中的 `module.prop` 里的 `description` 字段。
- 设备开机后，服务会先等待 `60` 秒，再进行首次节假日数据初始化，避免刚开机网络未就绪导致同步失败。
- 你可以在 Magisk 的模块列表里看到类似这些状态：
  - `当前状态: 服务启动中`
  - `当前状态: 开机等待 60 秒后初始化`
  - `当前状态: 服务运行中，下次触发: 今日 17:30`
  - `当前状态: 正在打开企业微信打卡界面 (08:10)`
  - `当前状态: 今日跳过打卡 (08:10，节假日/周末)`
- 如果 Magisk 当前页面没有自动刷新，返回模块页或重新打开 Magisk 后通常就能看到最新状态。

## 手动动作

- 模块新增了 `action.sh`，可在 Magisk 模块界面中手动执行。
- 这个动作会立即拉取**当年**节假日清单，并更新：
  - `holiday_year.txt` 缓存
  - `service.log` 日志
  - Magisk 里看到的模块状态
- `action.sh` 与 `service.sh` 现在共用 `common.sh`，下载、解析、状态更新逻辑保持一致。
- 适合用于以下场景：
  - 你刚装完模块，想立刻同步节假日数据
  - 你怀疑开机时网络未就绪，想手动重拉一次
  - 你想快速验证接口拉取和解析是否正常

## 注意

- 模块会尝试唤醒屏幕并打开考勤界面，但不会解除系统锁屏（PIN/图案/密码）。
- 模块现在会额外尝试 `dismiss-keyguard`、菜单键和上滑手势来处理**非安全锁屏**；如果设备启用了 PIN/图案/密码/指纹/面容等安全锁，脚本仍然无法绕过系统安全机制。
- 只要到达目标界面，企业微信即可继续完成自动打卡（前提是应用本身已配置好打卡逻辑）。
- 该服务在设备开机且模块启用后会常驻后台，所以每天 08:10 和 17:30 都能触发。
- 目标应用包名为 `com.tencent.wework`，请确保企业微信已安装。

## 为什么做这个模块

- TMD劳资早上来早了一点忘打卡了。
- TM这个月全勤奖没了。
